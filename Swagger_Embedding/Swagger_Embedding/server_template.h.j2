/* AUTO-GENERATED BY GENERATOR.PY - DO NOT EDIT */
#pragma once
#include <esp_http_server.h>

// 1. Function prototypes for your callbacks
typedef struct {
{% for path, methods in paths.items() %}
    {% for method, info in methods.items() %}
    esp_err_t (*on_{{ info.operationId }})(httpd_req_t *req);
    {% endfor %}
{% endfor %}
} api_callbacks_t;

// 2. The registration function
esp_err_t register_generated_routes(httpd_handle_t server, api_callbacks_t *cbs);